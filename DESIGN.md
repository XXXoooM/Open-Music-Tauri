# Open-Music 视觉设计规范

> 本文件描述每个视觉区域的具体尺寸、间距、层级和动效。
> 所有颜色/圆角/动效曲线引用 `src/styles/tokens.css` 中定义的变量，本文件不重复定义。
> 与 `CLAUDE.md` 冲突时，以 `CLAUDE.md` 为准。
>
> > 视觉参考图见 docs/screenshots/

---

## 一、设计原则

1. **克制**：留白比装饰重要。宁可少一个元素，不要多一个。
2. **层次**：通过材质（半透明+模糊）、明度、字重区分层级，不靠边框和阴影。
3. **一致**：所有圆角、间距、动效曲线来自同一套 Token。
4. **原生感**：窗口毛玻璃使用系统 vibrancy，不是 CSS 模拟的假模糊。
5. **内容优先**：专辑封面和歌词是主角，控制栏和装饰元素退居次要。

**参考基线**：Apple Music macOS 桌面端（Sonoma / Sequoia 版本）。

---

## 二、整体布局

### 2.1 窗口

- 最小尺寸：**1200 × 640**
- 默认尺寸：**1280 × 800**
- 圆角：macOS 由系统控制，Windows / Linux 使用 `--radius-lg` (20px)
- 标题栏：macOS 隐藏，红绿灯按钮悬浮在侧边栏顶部左侧

### 2.2 三段式结构

```
┌──────────────┬──────────────────────────────────────┐
│              │                                      │
│  Sidebar     │         MainContent                  │
│  240px 固定   │         flex-1 自适应                 │
│              │                                      │
│              │                                      │
├──────────────┴──────────────────────────────────────┤
│                                                     │
│              MiniPlayer                             │
│              高 72px，横跨整宽                        │
│                                                     │
└─────────────────────────────────────────────────────┘
```

- **Sidebar**：固定宽度 **240px**，`flex-shrink: 0`
- **MainContent**：`flex-1`，内部独立滚动
- **MiniPlayer**：固定高度 **72px**，`position: sticky; bottom: 0`
- 三者之间的分隔**不用边框线**，靠材质差异区分

### 2.3 全屏 Now Playing 页

点击 MiniPlayer 的封面区域，全屏展开覆盖整个窗口（包括侧边栏）。

- 覆盖层级：`z-index: 100`
- 关闭方式：左上角返回按钮 / `Esc` 键 / 向下滑动手势
- 过渡：0.4s `var(--ease-apple)`，从 MiniPlayer 位置"放大展开"

---

## 三、Sidebar（侧边栏）

### 3.1 材质与容器

- 背景：`var(--material-sidebar)`
- 模糊：`backdrop-filter: blur(var(--blur-sidebar))`
- 内边距：上 `48px`（给 macOS 红绿灯留位），左右 `12px`，下 `16px`

### 3.2 结构（从上到下）

**① Logo 区**（高度 56px）
- 应用名 "Open-Music"，字号 17px，字重 600，`var(--text-primary)`
- 左对齐，左内边距 16px

**② 主导航**（分组）
- 分组标题：字号 11px，字重 600，`var(--text-tertiary)`，大写字母 + `letter-spacing: 0.06em`
- 每项高度 **36px**，圆角 `var(--radius-sm)`，左右内边距 10px
- 图标尺寸 18px，图标与文字间距 10px
- 文字字号 14px，字重 500

导航项列表：
- 立即播放（PlayCircle）
- 音乐库（Library）— 展开后含：歌曲、专辑、艺术家、播放列表
- 浏览（Compass）
- 搜索（Search）

**③ 状态样式**
- 默认：`color: var(--text-secondary)`，无背景
- 悬停：背景 `rgba(255,255,255,0.06)`，文字变 `var(--text-primary)`，过渡 0.15s
- 激活：背景 `rgba(255,255,255,0.1)`，文字 `var(--text-primary)`，字重 600，左侧无额外标记

### 3.3 底部

- 设置入口（Settings 图标 + "设置"文字）
- 与上方导航间隔 8px，样式同导航项

---

## 四、MainContent（主内容区）

### 4.1 容器

- 背景：`var(--material-content)`
- 顶部内边距 `24px`，左右 `32px`，底部 `24px`
- 独立滚动，滚动条默认隐藏，滚动时淡入

### 4.2 顶部工具栏

- 高度**由标题撑开**（约 76px），使用 `flex` 布局固定在顶部（不用 `sticky`，避免与容器 padding 冲突）
- 内边距：上 24px、下 16px、左右 32px
- 左侧：大标题（如"歌曲"），字号 28px，字重 700，`var(--font-display)`
- 右侧：视图切换（列表/网格）、排序、搜索图标，图标 20px
- 滚动时自动添加底部渐隐遮罩（Phase 3 实现）

### 4.3 歌曲列表

每行结构：
```
[序号/播放图标] [封面 40x40] [标题 + 艺术家] [专辑] [时长]
```

- 行高 **56px**
- 封面圆角 `var(--radius-sm)`
- 标题字号 14px，字重 500，`var(--text-primary)`
- 艺术家字号 13px，字重 400，`var(--text-secondary)`
- 专辑名同艺术家，但列宽固定 180px
- 时长右对齐，`tabular-nums`，字号 13px，`var(--text-tertiary)`

**状态**：
- 默认：无背景
- 悬停：背景 `rgba(255,255,255,0.05)`，行内右侧"更多"按钮淡入
- 播放中：标题变 `var(--dynamic-accent)`，序号位显示跳动音波图标

### 4.4 专辑网格

- 每列最小宽度 180px，`gap: 24px`
- 卡片：封面（正方形，圆角 `var(--radius-md)`）+ 下方文字
- 封面上方悬停时浮出半透明播放按钮（48px 圆形，`var(--accent)` 底）
- 专辑名字号 14px，字重 500；艺术家字号 13px，`var(--text-secondary)`

---

## 五、MiniPlayer（迷你播放条）

### 5.1 材质与容器

- 背景：`var(--material-player)`
- 模糊：`backdrop-filter: blur(var(--blur-player))`
- 上下边框：`0.5px solid rgba(255,255,255,0.08)`（仅顶部）
- 内边距：左右 16px，高度 72px

### 5.2 三段式内部布局

```
[封面+信息 30%] [中央控制区 40%] [右侧功能区 30%]
```

**① 左侧信息区**
- 封面 48×48，圆角 `var(--radius-sm)`，点击展开全屏 Now Playing
- 标题：字号 14px，字重 500，单行省略
- 艺术家：字号 12px，`var(--text-secondary)`，单行省略
- 封面与文字间距 12px

**② 中央控制区**
- 按钮从左到右：随机（Shuffle）、上一首、播放/暂停、下一首、循环（Repeat）
- 图标尺寸：辅助按钮 18px，播放/暂停 20px
- 按钮间距 20px
- 播放/暂停按钮：44×44 圆形，背景 `var(--text-primary)`，图标 `#000`（唯一允许的黑色使用场景）
- 下方进度条：宽度与中央区等宽，高度 2px（悬停变 4px）
  - 已播放：`var(--text-primary)`
  - 未播放：`rgba(255,255,255,0.15)`
  - 悬停时显示圆形拖拽柄（10px，`var(--text-primary)`）
- 进度条两侧时间：字号 11px，`tabular-nums`，`var(--text-tertiary)`，宽度固定 40px

**③ 右侧功能区**
- 歌词（Mic2）、音量（Volume2）、队列（ListMusic）
- 图标 18px，间距 16px
- 音量图标悬停时展开滑条（宽 80px，样式同进度条）

### 5.3 状态

MiniPlayer **始终展示完整的三段式结构**（左侧信息 + 中央控制 + 右侧功能），
"未播放"通过状态样式表达，而不是隐藏结构。这样组件结构恒定，状态切换只是
颜色和文案的变化。

#### 未播放态

- **左侧**：
  - 封面占位为 48×48 半透明灰块（`var(--hover)`），无图片
  - 标题显示"未在播放"（`var(--text-primary)`）
  - 艺术家显示"请选择歌曲"（`var(--text-secondary)`）
- **中央**：
  - 辅助按钮（随机 / 上一首 / 下一首 / 循环）：`var(--text-tertiary)`
  - 播放按钮：背景 `rgba(255,255,255,0.15)`，图标色 `var(--text-secondary)`
  - 进度条：已播放 0%，底槽 `rgba(255,255,255,0.15)`
  - 时间：`00:00` / `00:00`（`var(--text-tertiary)`）
- **右侧**：
  - 三个图标：`var(--text-tertiary)`

#### 播放态

- **左侧**：
  - 封面显示真实图片
  - 标题显示歌曲名，艺术家显示歌手名
- **中央**：
  - 辅助按钮：`var(--text-secondary)`，悬停变 `var(--text-primary)`
  - 播放按钮：背景 `var(--text-primary)`（白），图标色为黑色
  - 进度条：已播放部分 `var(--text-primary)`，宽度跟随播放进度
  - 时间：显示当前时间 / 总时长
- **右侧**：
  - 三个图标：`var(--text-secondary)`，悬停变 `var(--text-primary)`

#### 通用

- 播放中封面可点击进入全屏 Now Playing
- 悬停反馈统一使用 `var(--duration-hover) var(--ease-apple)`
- 按钮点击反馈使用 `var(--duration-press) var(--ease-spring)`

#### 设计理由

MiniPlayer 是常驻组件，结构恒定可以减少 Phase 2 接入真实数据时的改动量。
"未播放"和"播放中"的差异只体现在**颜色、文案、进度值**三处，不涉及布局。

---


## 六、NowPlaying（全屏播放页）— 核心

### 6.1 整体布局

覆盖整个窗口（含 Sidebar 区域），`z-index: var(--z-overlay)`。

布局为**左右分栏**：

```
┌──────────────────────────────────────────────────────┐
│  ⌄                                         ▢ ─ ○ ✕ │  ← 顶部栏 64px
│                                                      │
│  ┌───────────┐    ┌──────────────────────────────┐  │
│  │           │    │                              │  │
│  │   封面     │    │         歌词区               │  │
│  │           │    │                              │  │
│  └───────────┘    └──────────────────────────────┘  │
│                                                      │
│   歌曲标题（大）                                      │
│   歌手名                                             │
│   专辑名                                             │
│                                                      │
│  ♥             ⏸              0:52 / 1:08           │  ← 底部栏 72px
│  ──────────────────────────────────────（进度线）    │
└──────────────────────────────────────────────────────┘
```

- **左侧区**：约 **40% 宽度**，垂直居中，含封面 + 歌曲信息
- **右侧区**：约 **60% 宽度**，歌词滚动区，垂直居中
- 两区之间间距 64px
- 整个页面水平内边距 64px

### 6.2 动态背景层

- 层级：`z-index: 0`（内容在 z-index: 1）
- 来源：`--dynamic-gradient`（由 `useDynamicColor` hook 注入）
- 模糊：`filter: blur(80px) saturate(1.6)`
- 缩放：`transform: scale(1.2)`（避免模糊后边缘透明）
- 不透明度：`0.85`
- 叠加径向遮罩让边缘略暗：
  `background: radial-gradient(ellipse at center, transparent 0%, rgba(0,0,0,0.35) 100%)`
- 过渡：切歌时 **1.5s** `var(--ease-apple)`
- **不使用**深灰底色遮挡，让封面色自然铺满

### 6.3 AlbumArt（专辑封面）

**尺寸**：`min(38vh, 360px)` 正方形

**形状**：方形圆角，`border-radius: var(--radius-lg)`（20px）

**阴影**：
```css
box-shadow:
  0 24px 72px rgba(0, 0, 0, 0.5),
  0 0 120px var(--dynamic-accent);
```

**切歌过渡**：0.6s `var(--ease-apple)`，新封面从 1.04 缩放到 1.0，透明度从 0 到 1

### 6.4 歌曲信息（封面下方）

- 与封面间距 **40px**
- 全部左对齐（与封面左边缘对齐）
- **标题**：字号 32px，字重 700，`var(--font-display)`，`var(--text-primary)`，单行省略
- **歌手**：字号 18px，字重 400，`rgba(255,255,255,0.9)`，与标题间距 12px
- **专辑**：字号 14px，`var(--text-tertiary)`，与歌手间距 6px

### 6.5 LyricsView（歌词区）

**尺寸与位置**：
- 宽度撑满右侧区
- 高度：**约 60vh**，`overflow: hidden`
- 上下无 `mask-image` 遮罩

**双层歌词结构**：

每行歌词包含两层：**原文** + **翻译**。若 LRC 没有翻译数据，则只显示原文层。

| 层 | 字号 | 字重 | 默认颜色 | 当前行颜色 |
|---|---|---|---|---|
| 原文 | 26px | 600 | `rgba(255,255,255,0.45)` | `var(--text-primary)` |
| 翻译 | 16px | 400 | `rgba(255,255,255,0.35)` | `rgba(255,255,255,0.85)` |

**层间距**：原文与翻译之间 8px

**行间距**：`padding: 14px 0`

**当前行样式**：
- 字号比默认大 15%（原文 26px → 30px，翻译 16px → 18px）
- 全部层提亮
- 原文加 `text-shadow: 0 0 20px rgba(255,255,255,0.2)`

**行过渡**：0.4s `var(--ease-apple)`

**滚动**：当前行位于歌词区垂直中心，`transform: translateY()` 实现，0.5s `var(--ease-apple)`

**逐词高亮**：由 AMLL 提供，当前行原文层使用平滑填充

**无歌词时**：显示"纯音乐，请欣赏"，`var(--text-tertiary)`，字号 18px

### 6.6 底部栏

高度 72px，位于页面底部，水平内边距 64px。

**三部分**：

**左**：
- 爱心图标（`Heart`），尺寸 22px
- 未收藏：`var(--text-secondary)`，仅描边
- 已收藏：`var(--accent)`，填充实心
- 悬停变 `var(--text-primary)`
- 点击切换收藏状态（Phase 4 实现逻辑）

**中**：
- 播放/暂停按钮，图标尺寸 32px
- 颜色 `var(--text-primary)`
- **无圆形背景**（与 MiniPlayer 不同）
- 悬停时图标轻微放大 1.05

**右**：
- 时间显示：`0:52 / 1:08`
- 字号 13px，`tabular-nums`，`var(--text-secondary)`

### 6.7 进度线

位于底部栏**下方**，紧贴窗口底边，高度 **2px**（悬停时 4px）。

- 已播放：`var(--text-primary)`
- 未播放：`rgba(255,255,255,0.15)`
- **无拖拽柄**
- 拖拽区域扩展为高度 12px 的透明点击区

### 6.8 顶部栏

高度 64px，左右内边距 24px。

**左**：
- 下拉箭头（`ChevronDown`），尺寸 24px
- `var(--text-primary)`
- 悬停时轻微缩放 1.1
- 点击关闭 NowPlaying

**右**（仅 Windows / Linux 显示，macOS 隐藏）：
- 窗口控制按钮组：最小化、最大化、关闭
- 图标尺寸 14px
- 按钮尺寸 24×24，间距 8px
- 悬停背景 `var(--hover)`
- 关闭按钮悬停背景 `rgba(255, 59, 48, 0.8)`

**顶部栏可拖拽**：整个顶部栏应用 `drag-region`，但按钮区域需加 `no-drag`。
---

## 七、SettingsPanel（设置面板）

### 7.1 容器

- 从右侧滑入的抽屉，宽度 **420px**
- 背景：`var(--material-content)` + `backdrop-filter: blur(50px)`
- 圆角：左侧 `var(--radius-lg)`
- 过渡：0.35s `var(--ease-apple)`

### 7.2 分区

每个设置分区：
- 标题：字号 13px，字重 600，`var(--text-tertiary)`，大写 + `letter-spacing`
- 与上一区间距 32px，与内容间距 12px

设置项行：
- 高度 44px，左侧标签（14px），右侧控件
- 控件类型：开关、下拉、滑条、颜色选择

### 7.3 必含设置项

**外观**
- 背景模式：渐变 / 流体 / 纯色 / 模糊（下拉）
- 主题：跟随系统 / 深色 / 浅色（下拉）
- 强调色来源：跟随封面 / 固定（下拉 + 色块）

**歌词**
- 字号：小 / 中 / 大（分段控件）
- 显示逐词高亮：开 / 关
- 隐藏无歌词歌曲的歌词区：开 / 关

**播放**
- 淡入淡出：开 / 关
- 交叉淡入时长：0-12s 滑条
- 无缝播放：开 / 关

---

## 八、动效规范

### 8.1 时长

| 场景 | 时长 | 曲线 |
|---|---|---|
| 悬停反馈 | 0.15s | `var(--ease-apple)` |
| 按钮点击 | 0.1s | `var(--ease-spring)` |
| 状态切换（播放/暂停） | 0.2s | `var(--ease-apple)` |
| 面板展开/收起 | 0.35s | `var(--ease-apple)` |
| 全屏播放页进出 | 0.4s | `var(--ease-apple)` |
| 封面切换 | 0.6s | `var(--ease-apple)` |
| 背景渐变切换 | 1.5s | `var(--ease-apple)` |

### 8.2 原则

- 所有 UI 反馈必须有动效，但不夸张
- 禁止使用 `bounce`、`elastic` 等夸张曲线（`--ease-spring` 除外，且只用于按钮点击）
- 禁止超过 0.6s 的 UI 反馈动画（背景渐变除外）
- 加载状态优先用骨架屏，不用 spinner

---

## 九、响应式

**本项目的窗口最小尺寸为 1200px**，DESIGN.md 早期设计的 1100px / 900px 断点不会触发。
响应式规则暂不需要实现。如果将来要支持更小的窗口，需要在 `tauri.conf.json` 里
降低 `minWidth`，并在 `global.css` 里补充对应的媒体查询。

---

## 十、图标规范

- 全部来自 `lucide-react`
- 尺寸：导航 18px，控制栏辅助 20-22px，播放/暂停 24-32px
- 描边：`stroke-width: 1.8`（lucide 默认 2，Apple 风格偏细）
- 颜色继承文字色，不单独指定

---

## 十一、状态与空态

**加载中**：使用骨架屏（半透明块 + 微光扫过），不用 spinner

**空状态**（如音乐库为空）：
- 居中图标（64px，`var(--text-tertiary)`）
- 主文案（16px，`var(--text-secondary)`）
- 副文案（13px，`var(--text-tertiary)`）
- 间距 16px

**错误状态**：使用 `var(--accent)` 色的图标 + 简短文案，不用弹窗

---

## 十二、禁止事项

1. 禁止纯黑 `#000` 作为大面积背景（播放/暂停按钮图标除外）
2. 禁止使用阴影做层级区分（除 AlbumArt 的柔和阴影）
3. 禁止边框线分隔区域（除 0.5px 的细微分隔）
4. 禁止彩色渐变按钮（保持 Apple Music 的克制）
5. 禁止超过 3 层的嵌套圆角
6. 禁止在歌词区外使用装饰性图形
7. 禁止圆角半径不一致（同一区域内的元素必须用同一级圆角）

---

## 十三、版本记录

| 日期 | 变更 |
|---|---|
| 2026-09-12 | 初稿建立 |

## 生成视觉组件后的自查

生成任何 UI 组件后，逐条检查：
- [ ] 所有颜色是否来自 tokens.css
- [ ] 圆角是否只用 --radius-sm/md/lg/full
- [ ] 是否使用了 DESIGN.md 中规定的尺寸和间距
- [ ] 动效曲线和时长是否匹配第八节
- [ ] 悬停/激活/禁用状态是否都实现了
- [ ] 是否违反了第十二节的禁止事项